import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

function RestockScreen() {
  return (
    <View>
      <Text>Restock Screen</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  root: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
})

export default RestockScreen
